cd vim-config
cp -f .vimrc "$HOME"
VundlePath="$HOME/.vim/bundle/Vundle.vim"
if [ -d "$VundlePath" ]; then
    echo "Installed"
elif [ ! -d "$VundlePath" ]; then
	git clone https://github.com/VundleVim/Vundle.vim.git "$VundlePath"
fi
cd "$HOME"
vim .vimrc +source % +PluginInstall +qall
