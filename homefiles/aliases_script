mv ~/.bashrc "$HOME"
ln -s ~/dotfiles/.bashrc ~/.bashrc
mv ~/.bash_aliases "$HOME"
ln -s ~/dotfiles/.bashrc ~/.bash_aliases
mv ~/.bash_ghusername "$HOME"
if [ -f ~/.bash_ghusername ]; then
    . ~/.bash_ghusername
fi
